@inherits LayoutComponentBase
@using PadelBracket.Services
@inject PlayerAccountService PlayerAccountService
@inject NavigationManager NavigationManager


<div class="app-shell">
    <aside class="app-sidebar">
        <NavMenu />
    </aside>

    <main class="app-main">
        <header class="app-topbar">
            <div>
                <span class="app-topbar-kicker">Panel de torneos</span>
            </div>

            <div class="topbar-account">
                @if (PlayerAccountService.IsLoggedIn && PlayerAccountService.CurrentPlayer != null)
                {
                    <a class="topbar-player-name" href="/profile">
                        @PlayerAccountService.CurrentPlayer.Name
                    </a>

                    <button class="btn btn-sm btn-outline-primary" @onclick="Logout">
                        Cerrar sesión
                    </button>
                }
                else
                {
                    <a class="btn btn-sm btn-outline-primary" href="/login">
                        Ingresar
                    </a>

                    <a class="btn btn-sm btn-primary" href="/register">
                        Registrarme
                    </a>
                }
            </div>
        </header>

        <section class="app-content">
            @Body
        </section>
    </main>
</div>

<div id="blazor-error-ui">
    Ocurrió un error inesperado.
    <a href="" class="reload">Recargar</a>
    <a class="dismiss">🗙</a>
</div>

@code {
    private void Logout()
    {
        PlayerAccountService.Logout();
        NavigationManager.NavigateTo("/");
    }
}